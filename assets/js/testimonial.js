const swiper = new Swiper(".testimnial-cards-main-container", {
  loop: true,
  slidesPerView: "auto",
  centeredSlides: false,
  grabCursor: true,
  // Ensure the loop works correctly with custom widths
  loopedSlides: 4,
});
